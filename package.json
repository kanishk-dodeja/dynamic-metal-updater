{
  "name": "dynamic-metal-price-updater-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix web && npm install --prefix web/frontend && npm run build --prefix web/frontend",
    "start": "node web/index.js"
  },
  "workspaces": [
    "web"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED"
}